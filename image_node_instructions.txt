# Open 5 terminal windows, 1 for each node.

# Run this command in all 5 windows.
cd ~/dragonpi/target_acquisition/ros_pyscripts/

# Run all these nodes, 1 in each terminal windows.
python camera.py
python servo.py
python image_processor.py -c classifiers/acid.xml classifiers/flammable.xml classifiers/misc.xml -r 1100
python image_writer.py -t images -o images/
python image_writer.py -t processed_images -o processed_images/

# Use to copy images from the pi to local machine.
rsync -r ubuntu@dragonpi:~/dragonpi/target_acquisition/ros_pyscripts/images/ ~/Desktop/images/
rsync -r ubuntu@dragonpi:~/dragonpi/target_acquisition/ros_pyscripts/processed_images/ ~/Desktop/processed_images/
